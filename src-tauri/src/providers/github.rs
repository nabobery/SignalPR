// GitHub adapter functions.
// PR metadata fetch is in commands/intake.rs (via gh CLI).
// Review submission is in commands/submission.rs (via gh CLI).
// This module reserved for future shared GitHub utilities (e.g., GraphQL inline comments in Phase 2).
