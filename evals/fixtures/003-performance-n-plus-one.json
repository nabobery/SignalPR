{
  "id": "003-performance-n-plus-one",
  "description": "TypeScript API handler with N+1 query pattern inside a loop.",
  "diff": "--- a/src/routes/orders.ts\n+++ b/src/routes/orders.ts\n@@ -1,3 +1,20 @@\n+import { db } from '../db';\n+import { Router } from 'express';\n+\n+const router = Router();\n+\n+router.get('/orders', async (req, res) => {\n+  const orders = await db.query('SELECT * FROM orders');\n+  const results = [];\n+  for (const order of orders) {\n+    const customer = await db.query(\n+      'SELECT * FROM customers WHERE id = $1',\n+      [order.customer_id]\n+    );\n+    results.push({ ...order, customer: customer[0] });\n+  }\n+  res.json(results);\n+});\n+\n+export default router;\n",
  "expected": {
    "min_findings": 1,
    "max_findings": 4,
    "must_mention_files": ["src/routes/orders.ts"],
    "severity_range": ["warning", "critical"],
    "must_contain_keywords": ["N+1", "loop", "query", "join"]
  }
}
